# OpenClaw Gateway 配置
# 注意：这里不需要配置，因为 LemClaw 直接使用 openclaw CLI 命令
# OPENCLAW_GATEWAY_URL 和 GATEWAY_TOKEN 不再需要

# 数据库配置
DATABASE_URL=sqlite:///auth_codes.db

# 服务器配置
HOST=0.0.0.0
PORT=8089
DEBUG=False

# 授权码配置
AUTH_CODE_LENGTH=32
DEFAULT_CODE_COUNT=50
